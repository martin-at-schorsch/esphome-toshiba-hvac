esphome:
  name: esp32c3-ble-proxy
  friendly_name: esp32c3-ble-proxy

esp32:
  board: esp32-c3-devkitm-1
  framework:
    type: esp-idf

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "PvhAsXXxXQkDHZrisoS5RcJe3b6tjD5KTGjzhs+ERac="

ota:
  platform: esphome
  password: "94ef9e7b8d097c574397aeca3fea8784"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Esp32C3-Ble-Proxy"
    password: "CmfeSDFXAs6i"


mqtt:
  broker: !secret mqtt_host
  username: !secret mqtt_username
  password: !secret mqtt_password

esp32_ble_tracker:
  scan_parameters:
    interval: 1100ms
    window: 1100ms
    active: true

bluetooth_proxy:
  active: true
    